# 第 1 课：main 包与标准输出

- 日期：2026-08-31
- 课程序号：第 1 课
- 知识点：`package main`、`main()` 与 `fmt.Println()`

## 用途

Go 可执行程序由 `main` 包启动。标准输出是验证程序流程和结果的最直接方式。

## 核心概念

- 可执行程序入口文件声明 `package main`。
- `import "fmt"` 引入格式化输入输出包。
- `func main()` 是程序入口函数。
- `fmt.Println()` 输出内容并自动换行。

## 最小代码示例

```go
package main

import "fmt"

func main() {
    fmt.Println("Go 第 1 课")
}
```

## 3～5 分钟练习

增加变量 `minutes`，输出“今天学习 10 分钟”。

## 参考答案

```go
minutes := 10
fmt.Println("今天学习", minutes, "分钟")
```

## 与上一课的联系

这是 Go（Golang）路线的第一课。下一课将学习变量声明、基本类型与零值。
